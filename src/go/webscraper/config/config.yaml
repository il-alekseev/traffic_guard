db_path: "data/ipinfo_lite.mmdb"
workers: 1000
request_timeout: 15s
content:
  strategies:
    - trafilatura
    - http
  archive_strategies:
    - wayback
    - archive.today
  min_text_length: 200
  max_content_length: 50000
  user_agents_path: data/user_agents.txt
  anti_bot_phrases_path: data/anti_bot_phrases.txt
  strip_json_fragments: true
logging:
  level: info
kafka:
  brokers:
    - "kafka:9092"
  group_id: "scraper-consumer_13"
  client_id: "scraper"
  input_topic: "url-processing-requests"
  metadata_topic: "url-metadata-results"
  content_topic: "url-content-analysis"
  commit_interval: 5s
  poll_timeout: 2s
  auth:
    username: ""
    password: ""
    sasl_mechanism: ""
    tls_enabled: false
http:
  address: ":8010"
  read_timeout: 5s
  write_timeout: 10s
  shutdown_timeout: 10s
